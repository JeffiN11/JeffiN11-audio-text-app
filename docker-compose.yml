services:
  api:
    build: .
    container_name: audio-text-api
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+asyncpg://audiouser:audiopass@db:5432/audiodb
      OLLAMA_BASE_URL: http://ollama:11434
      OLLAMA_MODEL: llama3
      WHISPER_MODEL: base
    depends_on:
      db:
        condition: service_healthy
      ollama:
        condition: service_started
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    container_name: audio-text-db
    environment:
      POSTGRES_USER: audiouser
      POSTGRES_PASSWORD: audiopass
      POSTGRES_DB: audiodb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U audiouser -d audiodb"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  ollama:
    image: ollama/ollama:latest
    container_name: audio-text-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    entrypoint: ["/bin/sh", "-c", "ollama serve & sleep 5 && ollama pull llama3 && wait"]
    restart: unless-stopped

volumes:
  postgres_data:
  ollama_data:
